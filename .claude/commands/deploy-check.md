Before I push to GitHub, check:
1. No hardcoded colors anywhere
2. No console.log statements left in
3. No TypeScript errors or `any` usage
4. All new components have proper prop types
5. Nothing obviously broken on mobile
6. No API keys or secrets in the code

Report issues by file and line number.